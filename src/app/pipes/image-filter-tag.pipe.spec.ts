import { ImageFilterTagPipe } from './image-filter-tag.pipe';

describe('ImageFilterTagPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageFilterTagPipe();
    expect(pipe).toBeTruthy();
  });
});
